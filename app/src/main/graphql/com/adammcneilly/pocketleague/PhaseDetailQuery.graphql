query PhaseDetailQuery($id: ID) {
    phase(id: $id) {
        name
        sets {
            nodes {
                fullRoundText
                displayScore
                winnerId
                slots {
                    entrant {
                        ...EventEntrantFragment
                    }
                    slotIndex
                }
            }
        }
    }
}