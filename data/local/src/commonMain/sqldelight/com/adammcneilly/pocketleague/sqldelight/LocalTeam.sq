CREATE TABLE localTeam(
  id TEXT NOT NULL PRIMARY KEY,
  name TEXT NOT NULL,
  imageURL TEXT
);

selectAll:
SELECT *
FROM localTeam;

insertFullTeamObject:
INSERT INTO localTeam(id, name, imageURL)
VALUES ?;

INSERT INTO localTeam(id, name, imageURL)
VALUES (1234, 'Knights', 'someImageUrl');
